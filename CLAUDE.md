# CLAUDE.md

See [AGENTS.md](AGENTS.md).

That file is the source of truth for how to work in this repository, and it is
the cross-tool convention — 60,000+ projects, native support in Claude Code,
Codex, Cursor, Aider, Copilot, Gemini CLI and others, and stewarded by the
Agentic AI Foundation under the Linux Foundation.

Nested `AGENTS.md` files in `cmd/tjo/` and in each of the four submodules take
precedence over the root one for work in those directories.

Maintaining a second copy here would only let the two drift.
